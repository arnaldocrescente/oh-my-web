// import fallback from '../locales/en/fallback.json';
import main from '../locales/en/main.json';
import cv from '../locales/en/cv.json';

const resources = {
  fallback: main,
  main,
  cv,
} as const;

export default resources;
